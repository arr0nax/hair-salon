<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>hair salon</title>
    <link rel="stylesheet" href="../css/styles.css" media="screen" title="no title" charset="utf-8">
</head>
<body>
    <h1>Al's Hair Salon</h1>
    <h3 class="welcome"><i>Welcome, Al</i></h3>
    <h2>Your Stylists</h2>
    <ul>
        {% for stylist in stylists %}
            <a href="/stylist/{{stylist.getId}}"><li>{{stylist.getName}}</li></a>
        {% endfor %}
    </ul>
    <h3 class="inline">Add a Stylist</h3>
    <form class="inline" action="/addstylist" method="post">
        <label for="name">Name:</label>
        <input type="text" name="name" value="">
        <button type="submit" name="button">Add</button>
    </form>

    <h2>Your Clients</h2>
    <ul>

        {% for client in clients %}
            {% if not client.checkStylistExists %}
                <li><a href="/client/{{client.getId}}">{{client.getName}}</a> doesn't have a stylist anymore!</li>
            {% else %}
                <li><a href="/client/{{client.getId}}">{{client.getName}}</a></li>
            {% endif %}
        {% endfor %}
    </ul>

    <h3 class="inline">Add A Client</h3>
    <form class="inline" action="/addclient" method="post">
        <label for="name">Name:</label>
        <input type="text" name="name" value="">
        <label for="stylist_id">Stylist:</label>
        <select class="" name="stylist_id">
            {% for stylist in stylists %}
                <option value="{{stylist.getId}}">{{stylist.getName}}</option>
            {% endfor %}
        </select>
        <button type="submit" name="button">Add Client</button>
    </form>


</body>
</html>
